{
  "compilerOptions": {
    "baseUrl": ".",  // Root directory
    "paths": {
      "@/*": ["./src/*"],  // For src folder
      "@/firebase": ["./firebase.js"]  // Alias to your firebase.js in the root
    }
  }
}
